import { LogoSwitcherDirective } from './logo-switcher.directive';

describe('LogoSwitcherDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoSwitcherDirective();
    expect(directive).toBeTruthy();
  });
});
